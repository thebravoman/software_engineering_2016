<%= form_for(testbasis) do |f| %>
  <% if testbasis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testbasis.errors.count, "error") %> prohibited this testbasis from being saved:</h2>

      <ul>
      <% testbasis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
