#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include <errno.h>

int main () {
	char buffer[2000];
	read(STDIN_FILENO, buffer, sizeof(buffer));
	
	printf("\n%ld" , strlen(buffer));
	return 0;
}
